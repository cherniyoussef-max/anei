import { notFound } from "next/navigation"; import { isLocale } from "@/lib/i18n"; import { requireAdminPermission } from "@/server/auth/session"; import { getCourseOperations } from "@/modules/admin/queries/admin-operations"; import { AdminMetricWorkspace } from "@/modules/admin/components/AdminMetricWorkspace";
export default async function Page({params}:{params:Promise<{locale:string}>}){const{locale}=await params;if(!isLocale(locale))notFound();await requireAdminPermission(locale,"courses.read");const d=await getCourseOperations(),a=locale==="ar";return <AdminMetricWorkspace locale={locale} eyebrow={a?"كتالوج التعليم":"Catalogue pédagogique"} title={a?"الدورات":"Formations"} description={a?"إدارة الأداء والنشر والمناهج.":"Pilotage des performances, publications et cursus."} metrics={[{label:a?"الإجمالي":"Total",value:d.total},{label:a?"منشورة":"Publiées",value:d.secondary},{label:a?"التسجيلات":"Inscriptions",value:d.tertiary},{label:a?"الشهادات":"Certificats",value:d.amount}]}/>;}
