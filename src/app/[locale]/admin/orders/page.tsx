import { notFound } from "next/navigation"; import { formatMillimes,isLocale } from "@/lib/i18n"; import { requireAdminPermission } from "@/server/auth/session"; import { getCommerceOperations } from "@/modules/admin/queries/admin-operations"; import { AdminMetricWorkspace } from "@/modules/admin/components/AdminMetricWorkspace";
export default async function Page({params}:{params:Promise<{locale:string}>}){const{locale}=await params;if(!isLocale(locale))notFound();await requireAdminPermission(locale,"orders.read");const d=await getCommerceOperations(),a=locale==="ar";return <AdminMetricWorkspace locale={locale} eyebrow={a?"العمليات التجارية":"Opérations commerciales"} title={a?"الطلبات":"Commandes"} description={a?"حالة الطلبات ومتابعة التحصيل.":"État des commandes et suivi de l’encaissement."} metrics={[{label:a?"الإجمالي":"Total",value:d.total},{label:a?"مدفوعة":"Payées",value:d.secondary},{label:a?"معلقة":"En attente",value:d.tertiary},{label:a?"الإيرادات":"Revenus",value:formatMillimes(d.amount,locale)}]}/>;}
