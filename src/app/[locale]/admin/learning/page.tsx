import { notFound } from "next/navigation"; import { isLocale } from "@/lib/i18n"; import { requireAdminPermission } from "@/server/auth/session"; import { getLearningOperations } from "@/modules/admin/queries/admin-operations"; import { AdminMetricWorkspace } from "@/modules/admin/components/AdminMetricWorkspace";
export default async function Page({params}:{params:Promise<{locale:string}>}){const{locale}=await params;if(!isLocale(locale))notFound();await requireAdminPermission(locale,"learning.read");const d=await getLearningOperations(),a=locale==="ar";return <AdminMetricWorkspace locale={locale} eyebrow={a?"تحليلات التعليم":"Analyse pédagogique"} title={a?"التعلّم":"Apprentissage"} description={a?"مؤشرات التسجيل والتقدم والإكمال من قاعدة البيانات.":"Suivi réel des inscriptions, de la progression et de la complétion."} metrics={[{label:a?"التسجيلات":"Inscriptions",value:d.total},{label:a?"مكتملة":"Terminées",value:d.secondary},{label:a?"متوسط التقدم":"Progression moyenne",value:`${d.tertiary}%`}]} note={a?"تعرض هذه المساحة المقاييس المتاحة فقط؛ لا يتم اختراع جلسات تعلم غير مسجلة.":"Cette vue n’affiche que les mesures persistées ; aucune session d’apprentissage non tracée n’est inventée."}/>;}
