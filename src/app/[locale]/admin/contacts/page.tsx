import { notFound } from "next/navigation"; import { isLocale } from "@/lib/i18n"; import { requireAdminPermission } from "@/server/auth/session"; import { getContentCounts } from "@/modules/admin/queries/admin-operations"; import { AdminMetricWorkspace } from "@/modules/admin/components/AdminMetricWorkspace";
export default async function Page({params}:{params:Promise<{locale:string}>}){const{locale}=await params;if(!isLocale(locale))notFound();await requireAdminPermission(locale,"contacts.read");const d=await getContentCounts(),a=locale==="ar";return <AdminMetricWorkspace locale={locale} eyebrow={a?"الدعم والعمليات":"Support & opérations"} title={a?"رسائل الاتصال":"Contacts"} description={a?"متابعة طلبات الاتصال المسجلة.":"Suivi des demandes de contact enregistrées."} metrics={[{label:a?"الرسائل":"Messages",value:d.contacts}]}/>;}
