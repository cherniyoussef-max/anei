import { notFound } from "next/navigation";
import { isLocale } from "@/lib/i18n";
import { env } from "@/server/env";

export default async function PrivacyPage({ params }: { params: Promise<{ locale: string }> }) {
  const { locale } = await params; if (!isLocale(locale)) notFound(); const ar = locale === "ar";
  return <section className="section"><article className="container article-shell"><span className="eyebrow">{ar ? "البيانات" : "Données personnelles"}</span><h1>{ar ? "سياسة الخصوصية" : "Politique de confidentialité"}</h1><p className="article-lead">{ar ? "توضح هذه الصفحة مبادئ المنصة بشأن جمع البيانات واستخدامها وحمايتها." : "Cette page présente les principes de la plateforme concernant la collecte, l’utilisation et la protection des données."}</p><h2>{ar ? "البيانات المستخدمة" : "Données utilisées"}</h2><p>{ar ? "تُستخدم بيانات الحساب والتسجيل والمشتريات والتقدم لتقديم الخدمة وتأمين الوصول. لا ينبغي جمع بيانات غير ضرورية." : "Les données de compte, d’inscription, d’achat et de progression sont utilisées pour fournir le service et sécuriser les accès. Les données inutiles ne doivent pas être collectées."}</p><h2>{ar ? "الأمان والاحتفاظ" : "Sécurité et conservation"}</h2><p>{ar ? "تطبق المنصة ضوابط وصول وتسجيلًا أمنيًا ونسخًا احتياطية. يجب اعتماد مدد الاحتفاظ النهائية بعد المراجعة القانونية قبل الإطلاق." : "La plateforme applique des contrôles d’accès, une journalisation de sécurité et des sauvegardes. Les durées définitives de conservation doivent être validées juridiquement avant lancement."}</p><h2>{ar ? "التواصل" : "Contact"}</h2><p>{env.CONTACT_EMAIL}</p><p className="small-muted">{ar ? "يجب مراجعة هذه السياسة من قبل المستشار القانوني للأكاديمية قبل الإنتاج." : "Cette politique doit être revue par le conseil juridique de l’Académie avant mise en production."}</p></article></section>;
}
